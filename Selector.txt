var titleNodeList = document.querySelector(base.elements.titleSelector);
var imageNodeList = document.querySelector(base.elements.imageSelector);
var descNodeList = document.querySelector(base.elements.descSelector);
var authorNodeList = document.querySelector(base.elements.authorSelector);
var ratingNodeList = document.querySelector(base.elements.ratingSelector);
var reviewNodeList = document.querySelector(base.elements.reviewSelector);
var basePriceNodeList = document.querySelector(base.elements.basePriceSelector);
var discPriceNodeList = document.querySelector(base.elements.discPriceSelector);
var durationNodeList = document.querySelector(base.elements.durationSelector);
var lectureNodeList = document.querySelector(base.elements.lectureSelector);
var levelNodeList = document.querySelector(base.elements.levelSelector); 
var urlNodeList = document.querySelector(base.elements.urlSelector);